---
import MainGridLayout from "@/layouts/MainGridLayout.astro";
import ArchivePanel from "@components/controls/ArchivePanel.svelte";
import { getSortedPostsList } from "@/utils/content-utils";

const sortedPostsList = await getSortedPostsList();
const lifeNotesPosts = sortedPostsList.filter(
  (post) => post.data.category?.trim() === "Life Notes",
);
---

<MainGridLayout title="Life Notes">
  <ArchivePanel sortedPosts={lifeNotesPosts} client:only="svelte" />
</MainGridLayout>
